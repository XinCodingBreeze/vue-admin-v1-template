# 生产环境配置
# 构建命令：vite build（默认使用 .env.production）

# 环境标识（非 'dev' 时，request.js 会使用 VITE_APP_API_URL 作为 baseURL）
VITE_APP_DEV='production'

# 生产环境 API 地址（部署时替换为实际的后端接口地址）
VITE_APP_API_URL='https://your-production-api.com'
